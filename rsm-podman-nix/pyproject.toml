[project]
name = "rsm-podman-nix-base"
version = "0.1.0"
description = "Base uv environment for the RSM Nix-based Podman image"
requires-python = ">=3.13,<3.14"
dependencies = [
  "ipykernel>=7.1.0",
  "ipython>=9.8.0",
  "requests>=2.32.5",
  "xgboost==2.0.3",
]

[tool.uv]
package = false
